"""Settings tab — Provider Integration Management control plane."""

from __future__ import annotations

from ui.settings import render_settings_panel


def render() -> None:
    render_settings_panel()
